import styled from "styled-components";

export const Container = styled.header`
    height: 50px;
    display: grid;
    place-items: center;
    padding: 10px 0;

    a{
        cursor: pointer;
    }
`