import React from 'react'

import {
  Container,
  MapBox
} from "./styled-MapContainer"

const MapContainer = () => {
  return (
    <Container>
      <MapBox>Mapa</MapBox>
    </Container>
  )
}

export default MapContainer